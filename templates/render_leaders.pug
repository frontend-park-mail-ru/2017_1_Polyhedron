if locals.items.length
    table.leaderboard
        each item, index in locals.items
            if user
                tr.leaderboard__leader.leader(class= (item.login === user.login) ? 'leaderboard__leader_current' : '')
            else
                tr.leaderboard__leader.leader
                    td.leader__info.leader__info_place= index + 1
                    td.leader__info.leader__info_login= item.login
                    td.leader__info.leader__info_score= item.score
else
    p.text-block Лидеров пока нет